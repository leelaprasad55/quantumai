services:
  - type: web
    name: quantumlearn
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_PATH
        value: /var/data/quantumlearn.db
    disk:
      name: quantumlearn-data
      mountPath: /var/data
      sizeGB: 1